{
  "name": "opehst-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app",
    "server",
    "shared"
  ],
  "scripts": {
    "start": "npm run start --workspace app",
    "android": "npm run android --workspace app",
    "ios": "npm run ios --workspace app",
    "web": "npm run web --workspace app"
  },
  "devDependencies": {
    "@types/react": "~19.2.14",
    "expo-router": "~56.2.5",
    "typescript": "~6.0.3"
  }
}
